(compile_programming_model)=

# smith.compile Programming Model

The `smith.compile` programming model:
1. Clarifies some internal behaviors of `smith.compile` so that one can better predict compiler behavior on user code and
2. Provides ways for one to take more fine-grained control over `smith.compile`.

By understanding the `smith.compile` programming model, one can systematically unblock themselves when encountering issues with `smith.compile`.

```{toctree}
programming_model.dynamo_core_concepts
programming_model.graph_breaks_index
programming_model.non_strict_tracing_model
programming_model.recompilation
programming_model.observability
programming_model.reporting_issues
```
