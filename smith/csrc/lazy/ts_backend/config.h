#pragma once
#include <c10/util/Flags.h>

// TODO(whc) unclear if this is useful, has only been tested as true
SMITH_DECLARE_bool(smith_lazy_ts_tensor_update_sync);

SMITH_DECLARE_bool(smith_lazy_ts_cuda);
